let JWT_SECRET,jwt;_ffb‍.x([["default",()=>_ffb‍.o]]);_ffb‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_ffb‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

class jwtServices{
    static sign(payload,secret=JWT_SECRET,expiry='60s'){
        return jwt.sign(payload,secret,{expiresIn:expiry});
    }
    static verify(token,secret=JWT_SECRET){
        return jwt.verify(token,secret);
    }
}
_ffb‍.d(jwtServices);